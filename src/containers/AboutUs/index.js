import React from 'react';
import Layout from '../../components/Layout';

 
const AboutUs = (props) => {
    console.log(props);
    return (
        <Layout>
        
        </Layout>
    );
}
 
export default AboutUs;